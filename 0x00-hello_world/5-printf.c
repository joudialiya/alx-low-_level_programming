#include <stdio.h>
/**
 * main - a program that print a str
 *
 * Return: 0
 */
int main(void)
{
	char *str = "with proper grammar, but the outcome is a piece of art,";

	printf("%s\n", str);
	return (0);
}
